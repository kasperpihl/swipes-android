<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/snooze_main_layout"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:gravity="center"
    android:orientation="vertical"
    tools:context="com.swipesapp.android.ui.activity.SnoozeActivity">

    <include
        layout="@layout/snooze_view"
        android:layout_width="@dimen/snooze_view_width"
        android:layout_height="@dimen/snooze_view_height" />

    <!-- Adjust time tip -->
    <TextView
        android:id="@+id/snooze_adjust_hint"
        style="?android:textAppearanceSmall"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="@dimen/snooze_adjust_hint_margin_bottom"
        android:background="@color/transparent"
        android:ellipsize="end"
        android:gravity="center"
        android:singleLine="true"
        android:text="@string/snooze_adjust_hint"
        android:visibility="gone" />

</LinearLayout>
